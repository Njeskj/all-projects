# Placeholder secret (plain values ok for local kind cluster demo).
# ponytail: plaintext in-repo secret for local kind only; use sealed-secrets/external-secrets for real envs.
apiVersion: v1
kind: Secret
metadata:
  name: ecommerce-secrets
  namespace: ecommerce
type: Opaque
stringData:
  POSTGRES_PASSWORD: "ecommerce"
  SPRING_DATASOURCE_PASSWORD: "ecommerce"
