# Placeholder secret; replace with a real secret manager reference before production use.
apiVersion: v1
kind: Secret
metadata:
  name: matching-secrets
type: Opaque
stringData:
  SPRING_DATASOURCE_USERNAME: "postgres"
  SPRING_DATASOURCE_PASSWORD: "postgres"
